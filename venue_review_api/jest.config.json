{
  "testEnvironment": "node",
  "preset": "ts-jest",
  "coveragePathIgnorePatterns": ["/node_modules/", "/dist", "/public"],
  "transform": {
    "^.+\\.ts?$": [
      "ts-jest",
      {
        "tsconfig": "tsconfig.build.json"
      }
    ]
  }
}
